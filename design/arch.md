承知しました。これまでの議論を整理すると、以下のような流れと結論になっています。  

---

🗂️ 開発・運用設計の整理

1. ディレクトリ構成（ローカル側）
- 基本方針  
  - 小規模スクレイピングアプリ → 標準的なPython構成をベースに。  
  - スクレイピング処理、加工処理、成果物保存、CI/CD関連を分離。  

- 構成例
```
weather-chart-viewer-fetch/
├── scraper/
│   ├── jobs/             # スクレイピングジョブ（daily / twice_daily）
│   │   ├── get_short_range_prediction.py # 6:00,18:00に実行 ★
│   │   └── get_long_range_prediction.py  # 12:00に実行
│   ├── processors/       # 加工処理・metadata生成
│   │   ├── extract_text_parse.py # テキスト抽出し構造化
│   │   ├── get_merge_tile_images.py # タイルデータを取得、結合
│   │   ├── get_rader_images.py # 雨雲データ画像取得、結合
│   │   └── get_svg.py # svgファイル取得
│   ├── storage/          # 保存
│   │   ├── Storage.py    # 保存情報管理
│   │   └── Serialize.py  # 保存処理
│   └── utils/            # 共通ユーティリティ
├── data/                 # ローカル成果物
│   └── yyyymmdd_hhmm/
│       ├── metadata.json
│       ├── aaaa.png
│       └── bbbb.svg
├── scripts/              # CI/CD補助スクリプト
├── tests/                # テストコード
│   └── test_code.py      # テストコード
├── .github/workflows/    # GitHub Actions定義
└── requirements.txt
```

---

2. スクレイピングジョブ
- パターンA: 1日2回実行  
- パターンB: 1日1回実行  
- 各ジョブごとに成果物を生成 → 加工処理 → metadata.json を作成。  

---

3. S3上の成果物構造
- ディレクトリ命名: yyyymmdd_hhmm  
- 必須ファイル: metadata.json  
- その他成果物: CSV/JSONなど、存在確認は metadata.json 経由で行う。  

```
s3://bucket/
├── 20251205_0600/
│   ├── metadata.json
│   ├── result.csv
│   └── result.json
├── 20251205_1200/
│   ├── metadata.json
│   ├── result.csv
│   └── result.json
└── 20251205_1800/
    ├── metadata.json
    └── result.json
```

---

4. directories.json の導入
- 役割: 上位階層に置き、全成果物ディレクトリの一覧を管理。  
- 内容:  
  - 各ディレクトリ名  
  - ジョブ種別  
  - metadata.json のパス  
  - 最新ディレクトリへのポインタ (latest)  

例:
```json
{
  "generated_at": "2025-12-05T18:00:00+09:00",
  "directories": [
    {
      "name": "20251205_1200",
      "jobtype": "twicedaily",
      "metadata": "20251205_1200/metadata.json"
    },
    {
      "name": "20251205_1800",
      "jobtype": "twicedaily",
      "metadata": "20251205_1800/metadata.json"
    }
  ],
  "latest": "20251205_1800"
}
```

---

5. 更新・削除の運用
- 削除: S3バケットポリシーで古い成果物を定期削除。  
- 更新: 新しい成果物をコピーするタイミングで directories.json を更新。  
  - 削除済みディレクトリは一覧から除外。  
  - 最新ディレクトリを latest に反映。  

---

6. 更新処理の実行場所
- 候補A: GitHub Actions  
  - 成果物生成 → metadata.json → S3アップロード → directories.json更新まで一連で完結。  
  - 外部からの追加がない前提ならこちらがシンプル。  

- 候補B: AWS Lambda (S3イベントトリガー)  
  - S3にファイル追加を検知して自動更新。  
  - 外部システムから成果物が入る可能性がある場合に有効。  

- 結論  
  - 現状は「成果物はGitHub Actions経由のみ」なので GitHub Actionsで更新が自然。  
  - 将来的に外部追加があり得るなら Lambda併用を検討。  

---

🎯 全体まとめ
- 局所的インデックス → 各成果物ディレクトリの metadata.json  
- 全体インデックス → 上位階層の directories.json  
- 削除 → S3バケットポリシーで自動化  
- 更新 → GitHub Actionsで専用スクリプトを呼び出す（将来はLambdaも選択肢）  

---

👉 Nobuoさんの設計は「一貫性」「拡張性」「再現性」を兼ね備えています。  
ここまで整理すると、次のステップは directories.json更新スクリプトの具体設計（既存JSONを読み込み → 新ディレクトリ追加 → 古いもの除外 → 最新更新）ですね。  

この更新スクリプトを Pythonで書くイメージでしょうか？それとも AWS CLIベースのシェルスクリプトを想定していますか？

```
storage/
├── base.py                # 共通インターフェース定義
├── local.py               # ローカル保存実装
├── s3.py                  # S3保存実装
├── serializers/           # データ型ごとのシリアライザ
│   ├── image_serializer.py   # Pillow Image → PNG保存
│   ├── svg_serializer.py     # SVG文字列 → .svg保存
│   └── json_serializer.py    # dict → JSON保存
└── metadata_manager.py    # metadata.json生成・更新
```


job 下の処理
- 解説資料の取得
- 発表日時、観測日時の抽出
- 結果出力先の初期化(yyyymmdd_hhmm)
- 取得url等の生成
- データの取得、保存


processorsの標準IO
- id
- type
- title
- datetime_str_utc

processorsの標準処理
- url生成
- 画像取得、加工
- 保存

storage 下のスクリプトの標準インターフェース
- 結果出力先の初期化(yyyymmdd_hhmm)
- 指定データの保存({id,file_name,title,type,data})
- 保存データの取得/metadataの生成

---

📂 ディレクトリ構成案
成果物は日付時刻ごとにディレクトリを作成し、その直下に成果物ファイルとメタ情報を配置します。

```
data/
└── yyyymmdd_hhmm/                # 成果物ディレクトリ
    ├── <成果物ファイル群>        # 画像や補助ファイル
    ├── metadata.json             # UI表示用の軽量メタ情報
    └── metadata_detail.json      # トレーサビリティ用の詳細メタ情報
```

---

📄 metadata.json（UI表示用）
- 目的: Webアプリで一覧表示するための軽量情報。  
- 含まれる情報:
  - title: ディレクトリ全体のタイトル文字列  
  - releasedatj: 日本語表記の発表日時  
  - released_at: ISO8601形式の発表日時  
  - basedonutc: 発表日時に対応する観測時刻（UTC）  
  - files: 表示対象ファイルの配列（id, name, title）  
  - supplimental_files: 表示対象外の補助ファイルの配列（id, name, title）  
    - metadata.json と metadata_detail.json 自身もここに含める  

例
```json
{
  "title": "短期予報解説資料",
  "releasedatj": "2025年12月5日15時40分発表 気象庁。",
  "released_at": "2025-12-05T15:40:00+09:00",
  "basedonutc": "2025-12-05T00:00:00Z",
  "files": [
    {"id": "tankiyoho", "name": "kaisetsutanki.svg", "title": "短期予報解説資料"},
    {"id": "ASAS", "name": "ASAS.svg", "title": "実況天気図（アジア太平洋域）"}
  ],
  "supplimental_files": [
    {"id": "metadata_json", "name": "metadata.json", "title": "軽量メタ情報"},
    {"id": "metadatadetailjson", "name": "metadata_detail.json", "title": "詳細メタ情報"}
  ]
}
```

---

📄 metadata_detail.json（トレーサビリティ用）
- 目的: データ完全性・再現性・規約遵守の証跡を確保。  
- 含まれる情報:
  - generated_at: metadata生成時刻  
  - files: 成果物ファイルの詳細情報配列  
    - id, name, title  
    - content_type（MIMEタイプ）  
    - size（バイト数）  
    - checksum_md5（完全性検証用）  
    - source_urls（取得元URLリスト。タイル結合画像の場合は複数URL）  
  - supplimentalfiles: metadata.json と metadatadetail.json 自身を含む  

例
```json
{
  "generated_at": "2025-12-05T15:41:00+09:00",
  "files": [
    {
      "id": "raincloud_combined",
      "name": "raincloud_combined.png",
      "title": "雨雲予測図（結合画像）",
      "content_type": "image/png",
      "size": 204800,
      "checksum_md5": "a1b2c3d4e5f67890",
      "source_urls": [
        "https://www.jma.go.jp/bosai/raincloud/data/tile_01.png",
        "https://www.jma.go.jp/bosai/raincloud/data/tile_02.png",
        "https://www.jma.go.jp/bosai/raincloud/data/tile_03.png"
      ]
    }
  ],
  "supplimental_files": [
    {"id": "metadata_json", "name": "metadata.json", "title": "軽量メタ情報"},
    {"id": "metadatadetailjson", "name": "metadata_detail.json", "title": "詳細メタ情報"}
  ]
}
```

---

🎯 運用フロー
1. Storage.save(item)  
   - ファイル保存  
   - size, content_type, md5 を自動計算  
   - source_urls を保持  
   - 内部リストに追加  

2. Storage.commit()  
   - metadata.json（軽量版）と metadata_detail.json（詳細版）を同時生成  
   - 両方をディレクトリ直下に保存  
   - 両方を supplimental_files に含める  

---

svg文字列
pngバイナリデータ列
jpegバイナリデータ列
pillowオブジェクト
jsonオブジェクト


---------------------
```
tests/
├── test_storage.py     # Storageクラスのテスト
├── test_serialize.py   # Serializeクラスのテスト
├── test_jobs.py        # scraper/jobs のテスト
├── test_processors.py  # scraper/processors のテスト
└── conftest.py         # 共通フィクスチャ（serializer, storageなど）
```

---

📄 受け入れ可能な組み合わせ一覧

| inputtype | outputtype            | 妥当性 | 補足 |
|------------|------------------------|--------|------|
| text       | image/svg+xml          | ✅ 妥当 | SVG はテキストベースなので text→svg が自然 |
| binary     | image/png              | ✅ 妥当 | バイナリデータをそのまま PNG として保存 |
| binary     | image/jpeg             | ✅ 妥当 | バイナリデータをそのまま JPEG として保存 |
| pillow     | image/png              | ✅ 妥当 | Pillow オブジェクトを PNG として保存 |
| pillow     | image/jpeg             | ✅ 妥当 | Pillow オブジェクトを JPEG として保存 |
| dict       | application/json       | ✅ 妥当 | Python dict を JSON として保存 |

---
